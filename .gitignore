.venv/
output/
# data/ is committed (Open-Meteo rate-limits CI's shared IP) — refresh locally.
__pycache__/
*.pyc
.ruff_cache/
era5_staging/
# per-machine fleet shard assignment (see tools/daily-chunk.sh)
.gather-shard
payloads/

# Scratch dir for tools/extract_generated_js.py (see .github/workflows/lint.yml).
.lint-js/
